import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.shield.kenyonbwrs',
  appName: 'Shield',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
